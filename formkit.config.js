import { generateClasses } from "@formkit/themes"

const config = {
    config: {
        classes: generateClasses({
            global: {
                // wrapper: 
                // message: 
                
                // // asi puedo generar con css y la clase aa puedo ponerla en el archivo donde esta formkit o en cualqueir otro
                // // message: 'aa'
                // label: 
                // input: 
            },
            submit: {
                // input: 
            }
        })
    }
}

export default config